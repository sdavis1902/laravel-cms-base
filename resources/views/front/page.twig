{% extends 'front/layouts/' ~ page.layout.url ~ '.twig' %}

{% block content %}
    <div class="container">

        <div class="row">
            <div class="col-lg-12 text-center">
                <h1>{{ page.name }}</h1>
            </div>
        </div>
        <div class="row">
            <div class="col-lg-12">
                {{ page.content|raw }}
            </div>
        </div>

        <!-- /.row -->

    </div>
{% endblock %}
